# Project Memory Index

- Canonical project snapshot and engineering rules live in `CONTEXT.md` and `CONVENTIONS.md`; consult them before non-trivial work.
- For the Java 21/JDTLS classfile mismatch and required clean Maven verification after Java or POM edits, read `mem:tooling/java-lsp-target-class-version`.
- For memory graph structure, durability thresholds, and maintenance commands, read `mem:memory_maintenance`.
